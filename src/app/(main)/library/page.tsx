/**
 * Library — coming in Step 18.
 * Placeholder — full UI built in a later Build Order step.
 */
export default function LibraryPage() {
  return (
    <main className="flex flex-1 items-center justify-center p-6">
      <p className="text-muted">Library — coming in Step 18.</p>
    </main>
  )
}
