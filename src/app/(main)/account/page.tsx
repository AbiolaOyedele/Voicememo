/**
 * Account — coming in Step 21.
 * Placeholder — full UI built in a later Build Order step.
 */
export default function AccountPage() {
  return (
    <main className="flex flex-1 items-center justify-center p-6">
      <p className="text-muted">Account — coming in Step 21.</p>
    </main>
  )
}
