/**
 * Record — coming in Step 12.
 * Placeholder — full UI built in a later Build Order step.
 */
export default function RecordPage() {
  return (
    <main className="flex flex-1 items-center justify-center p-6">
      <p className="text-muted">Record — coming in Step 12.</p>
    </main>
  )
}
